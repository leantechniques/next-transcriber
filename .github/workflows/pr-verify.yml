name: PR Verification

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

jobs:
  verify_pr:
    name: Verify PR
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
